public ListNode Reverse(ListNode head){
	if ( head == null || head.next == null ) { 
		return head;
	}

	ListNode nxt;
	ListNode curr = head;
	ListNode pre = null;

	while ( curr != null ) {
		nxt = curr.next;
		pre = curr;
		curr = 
		
	}
	return pre;
}